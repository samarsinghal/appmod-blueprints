receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317

processors:

exporters:
  debug:
    verbosity: normal
  awsxray:
      region: us-west-2

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: []
      exporters: [awsxray, debug]